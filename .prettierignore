# Dependencies
node_modules/
.pnpm-store/
pnpm-lock.yaml
package-lock.json
yarn.lock

# Build outputs
dist/
build/
.next/
.turbo/
out/

# Generated by the TanStack Router plugin on every dev/build/vitest run, which
# reads `vite.config.ts`. Its own header asks to be excluded from the linter and
# the formatter; eslint.config.mjs already ignores it. Formatting it means the
# plugin's next write reverts prettier's, so the file oscillates and every run
# shows a spurious multi-hundred-line diff. `--cache` hides this until a miss.
**/routeTree.gen.ts

# Misc
.vscode/
coverage/
.DS_Store
*.log

**/.venv/**
